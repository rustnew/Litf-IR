[package]
name = "lift-opt"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
readme = "../../README.md"
description = "LIFT compiler pass manager: 13 optimisation passes — constant folding, DCE, CSE, tensor fusion, gate cancellation, qubit routing"
keywords = ["optimisation", "compiler", "passes", "fusion", "quantum"]
categories = ["compilers", "science"]

[dependencies]
lift-core = { workspace = true }
lift-quantum = { workspace = true }
tracing = { workspace = true }
